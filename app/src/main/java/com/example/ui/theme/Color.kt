package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// =========================================================================================
// DYNO LITE - PALETA OFICIAL DA MARCA
// =========================================================================================

// Fundos e Superfícies
val DynoBackground = Color(0xFF0D1017)       // Background principal (Grafite quase preto / Obsidian)
val DynoSurface = Color(0xFF242832)          // Surface / cartões padrão
val DynoSurfaceElevated = Color(0xFF303540)  // Surface elevada / dialogs / chips ativos
val DynoSurfaceContainer = Color(0xFF1B1F27) // Contêiner interno de dados / tabelas

// Cores de Ação e Marca
val DynoBluePrimary = Color(0xFF2F80ED)      // Azul principal (botões, seleção ativa)
val DynoBlueLight = Color(0xFF9DBEFF)        // Azul claro (destaques sutis, badges)

// Cores Semânticas do Dinamômetro (Métricas exclusivas)
val DynoPowerCyan = Color(0xFF39C6F4)        // Potência (exclusivamente para HP, cv, curva de potência)
val DynoTorqueOrange = Color(0xFFFF8A3D)     // Torque (exclusivamente para kgfm, Nm, curva de torque)

// Estados de Feedback e Qualidade
val DynoSuccessGreen = Color(0xFF42C77A)     // Sucesso (teste válido, itens aprovados, medição ativa)
val DynoWarningYellow = Color(0xFFF2C94C)    // Atenção (verificação, teste regular, aviso de calibração)
val DynoErrorRed = Color(0xFFE35D62)         // Erro / Cancelar (teste inválido, erro, botões destrutivos)

// Tipografia e Legibilidade
val DynoTextPrimary = Color(0xFFF2F4F8)      // Texto principal de alto contraste
val DynoTextSecondary = Color(0xFFB7BCC7)    // Texto secundário e legendas
val DynoTextMuted = Color(0xFF7A8290)        // Texto terciário e desabilitado

// Bordas e Divisores
val DynoBorder = Color(0xFF303540)           // Borda padrão sutil
val DynoBorderLight = Color(0xFF383E4D)      // Borda com destaque sutil
val DynoDivider = Color(0xFF1E232D)          // Divisores horizontais
